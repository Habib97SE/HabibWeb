{{> admin_header}}

<main class="container">
    <div class="row">
        <div class="columns col-12">
            {{> admin_sidebar}}
            <div class="column col-9">
                <article class="card">
                    <div class="card-image">
                        <img src="{{blog.blog.main_image}}" alt="{{blog.title}}" class="img-responsive">
                    </div>
                    <div class="card-header">
                        <h1>{{blog.blog.title}}</h1>
                    </div>
                    <div class="card-body">
                        {{{blog.blog.content}}}
                    </div>
                    <div class="card-footer columns col-12">
                        <div class="column col-6">
                           <span class="label my-2">
                           Published on {{blog.blog.created_at}}
                       </span>
                            <span class="label my-2">
                            Last updated on {{blog.blog.updated_at}}
                       </span>
                        </div>
                        <div class="column col-6">
                            <span class="label my-2">
                                Author: {{#if blog.blog.first_name}}
                                {{blog.blog.first_name}} {{blog.blog.last_name}}
                            {{else}}
                                {{blog.blog.author_first_name}} {{blog.blog.author_last_name}}
                            {{/if}}
                            </span>
                            <span class="label my-2">
                                {{#if blog.blog.author_email}}
                                    <a href="mailto:{{blog.blog.author_email}}">{{blog.blog.author_email}}</a>
                                {{/if}}
                            </span>
                        </div>
                    </div>
                </article>
            </div>
        </div>
    </div>
</main>

{{> admin_footer }}